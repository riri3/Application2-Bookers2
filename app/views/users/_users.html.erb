<h1>User info</h1>
 <table class="table">
  <tr><%= attachment_image_tag user, :profile_image, 500, 500, format: 'jpeg' %></tr>
  <tr>
    <th>name</th>
    <th><%= user.name %></th>
  </tr>
  <tr>
    <th>introduction</th>
    <th><%= user.introduction %></th>
   </tr>
  </thead>
 </table>

<%= link_to edit_user_path(user), method: :get do %>
<i class="btn btn-outline-secondary btn-block fas fa-user-cog"></i>
<% end %>