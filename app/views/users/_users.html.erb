<h2>User info</h2>
 <table class="table">
  <tr><%= attachment_image_tag user, :profile_image, format: 'jpeg', size: "100x100", fallback: "no_image.jpg" %></t fallback: "no_image.jpg"r>

  <tr>
    <th>name</th>
    <th><%= user.name %></th>
  </tr>
  <tr>
    <th>introduction</th>
    <th><%= user.introduction %></th>
   </tr>
  </thead>
 </table>

  <%= link_to edit_user_path(user), method: :get do %>
    <i class="btn btn-outline-secondary btn-block fas fa-user-cog"></i>
  <% end %>